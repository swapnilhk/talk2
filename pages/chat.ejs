<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "http://www.w3.org/TR/html4/loose.dtd">
<html>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=ISO-8859-1">
<title>Chat</title>
<script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
<script src="/socket.io/socket.io.js"></script>
<link rel="stylesheet" href="../css/styles.css" media="screen" type="text/css" />
<script>
	$(document).ready(function(){
		var uid = "<%=uid%>";
		var fuid = "<%=fuid%>";
		var fname = "<%=fname%>";
		var tid = "<%=tid%>";
		var server = io.connect("http://localhost:8080");
		//alert("sdq");
		
		$('#sendchat').click(function(){	
			var message = $('#message').val();
			$("#chat-area").append('<p>You: '+message+'</p>');
			server.emit('message', {'message':message, 'uid': uid, 'fuid': fuid, 'tid': tid});
			//alert("message: "+message);
		});
	});	
</script>
</head>
<body>
	<div id="page-wrap">
		<h2>Welcome to Chat Page</h2>
		<a href="/recieved_broadcasts">Recieved Brodcasts</a><a href="/sent_broadcast">Sent Broadcasts</a><a href="/threads">Threads</a><a href="/logout">Logout</a><br>
		<p id="name-area"></p>
		<div id="chat-wrap">
			<div id="chat-area">
			
			<%
				for(i in messages){
					%><p><%=messages[i].first_name+" "+messages[i].last_name+" : "+messages[i].msg%></p><%
				}
			%>
			
			</div>
			
		</div>		
		<div id="chatform">
			<textarea id="message" id="sendie" maxlength = '100' placeholder="Write something.."></textarea>
			<button id="sendchat">Send</button>
		</div>
	</div>
	
</body>

</html>
